pipeline:
  build:
    image: docker
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    commands:
      - docker --tls=false ps
  build_image:  
    image: plugins/docker
    #volumes: ["/var/run/docker.sock:/docker.sock"]
    #privileged: true
    commands:     
      - docker info
      - docker build -t build-lazarus-windows .
      